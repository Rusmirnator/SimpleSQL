import { VisibilityChangedDirective } from './visibility-changed.directive';

describe('VisibilityChangedDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilityChangedDirective();
    expect(directive).toBeTruthy();
  });
});
