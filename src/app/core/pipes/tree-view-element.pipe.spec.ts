import { TreeViewElementPipe } from './tree-view-element.pipe';

describe('TreeViewElementPipe', () => {
  it('create an instance', () => {
    const pipe = new TreeViewElementPipe();
    expect(pipe).toBeTruthy();
  });
});
